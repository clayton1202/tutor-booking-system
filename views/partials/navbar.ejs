<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">
            📚 Tutor Booking
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#mainNavbar" aria-controls="mainNavbar"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
                <% if (user) { %>
                    <li class="nav-item">
                        <span class="navbar-text text-light d-block d-lg-inline">
                            Hi, <%= user.name %>
                            <span class="badge bg-secondary text-uppercase"><%= user.role %></span>
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="/<%= user.role %>/dashboard">Dashboard</a>
                    </li>
                    <% if (user.role === 'student') { %>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="/student/tutors">Browse Tutors</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="/student/bookings">My Bookings</a>
                        </li>
                    <% } else if (user.role === 'tutor') { %>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="/tutor/schedule">My Schedule</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="/tutor/profile">My Profile</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="btn btn-outline-light btn-sm ms-lg-2" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link text-light" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-primary btn-sm ms-lg-2" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
